{
  "version": 1,
  "description": "github_core CI record (req-boot-bootstrap-ci-record, tap#365) — the stack this plugin's own tests run in, and the one claim per-plugin CI is allowed to make: that github_core is correct against the dependencies it DECLARES and nothing else. Install set is exactly the depends_on closure (identity_core, git_core, compliance_core) plus github_core itself; the consumer flips the self entry to editable so the CHECKOUT under test runs, not the pinned revision. Population seeds github_core (seed-only, offline). DELIBERATELY EXCLUDED: every other plugin, all credentials, all required_secrets, and any population step that reaches the network — so a fork can run this lane and a nightly cannot flake on a third party (req-boot-bootstrap-ci-record-5). Reproduce CI exactly: spawn --from 'git+https://github.com/unified-systems-com/tap-plugin-github-core@v0.6.0#ci' --dev-plugins github_core",
  "install": {
    "plugins": [
      {
        "slug": "identity_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-identity-core",
          "rev": "v0.1.2"
        },
        "note": "Declared in depends_on. Pinned at v0.1.2, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "git_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/git-core-tap",
          "rev": "v0.1.0"
        },
        "note": "Declared in depends_on. Pinned at v0.1.0, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "compliance_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-compliance-core",
          "rev": "v0.3.0"
        },
        "note": "Declared in depends_on. Pinned at v0.2.2, the release current on 2026-09-09. Public repo, no credential (req-tap-plugin-arch-source-secret-5)."
      },
      {
        "slug": "github_core",
        "enabled": true,
        "source": {
          "type": "git",
          "url": "https://github.com/unified-systems-com/tap-plugin-github-core",
          "rev": "v0.6.0"
        },
        "note": "Self, pinned like any app-of-apps record — the CONSUMER (plugin-ci boot-and-test, or `spawn --dev-plugins github_core`) substitutes the checkout under test via tap.dev_workspace (req-boot-bootstrap-ci-record-4). Booting this record unflipped runs the pinned revision."
      }
    ]
  },
  "population": {
    "on_failure": "abort",
    "steps": [
      {
        "type": "seed-plugin",
        "plugin": "github_core",
        "enabled": true,
        "note": "Own GRIFT: seeding it proves the bundle parses and its panel types resolve against a real registry."
      }
    ]
  }
}
