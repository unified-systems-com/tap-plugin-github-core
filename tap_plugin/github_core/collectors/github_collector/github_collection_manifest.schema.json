{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "github_core Collection Manifest",
  "type": "object",
  "additionalProperties": false,
  "required": ["manifest_version", "sources", "edges"],
  "properties": {
    "manifest_version": {"type": "string", "enum": ["0"]},
    "description": {"type": "string"},
    "api_base_url": {"type": "string"},
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "kind"],
        "properties": {
          "name": {"type": "string", "minLength": 1},
          "kind": {"enum": ["rest_endpoint", "repo_file", "custom_fn"]},
          "path": {"type": "string"},
          "path_pattern": {"type": "string"},
          "fallback_path": {"type": "string"},
          "query": {"type": "object", "additionalProperties": {"type": "string"}},
          "item_path": {"type": "string"},
          "target_entity_type": {"type": "string"},
          "target_field": {"type": "string"},
          "fields": {"type": "array", "items": {"type": "string"}},
          "permission_failure": {"enum": ["abort", "degrade_with_warning"]},
          "description": {"type": "string"}
        }
      }
    },
    "edges": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["slug", "source", "target"],
        "properties": {
          "slug": {"type": "string", "pattern": "^[A-Z][A-Z_]+__[a-z0-9_]+$"},
          "source": {"type": "string"},
          "target": {"type": "string"},
          "condition": {"type": "string"}
        }
      }
    }
  }
}
