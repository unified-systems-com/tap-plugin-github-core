{
  "slug": "ENABLED_ON__github_core",
  "name": "Enabled On",
  "description": "A GitHub App or first-party platform service is enabled on a repository — a github_app (e.g. Dependabot) or the Actions OIDC issuer (token.actions.githubusercontent.com), which every repo's workflows use to mint identity tokens.",
  "sources": [
    "github_core__github_app",
    "identity_core__oidc_issuer"
  ],
  "targets": [
    "github_core__github_repository"
  ],
  "default_dimensions": {
    "github.platform": "github.com",
    "github.surface": "apps"
  }
}
