{
  "slug": "PROTECTS__github_core",
  "name": "Protects",
  "description": "A ruleset gates a repository, or a specific ref within it. One ruleset defined at the organization protects many repositories, so this is the edge that carries application while the ruleset node carries definition.",
  "sources": [
    "github_core__github_ruleset"
  ],
  "targets": [
    "github_core__github_repository",
    "github_core__git_ref"
  ],
  "default_dimensions": {
    "github.platform": "github.com",
    "github.surface": "rules"
  },
  "property_schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "ref_pattern": {
        "type": "string",
        "description": "The condition token or fnmatch pattern that selected the target (`~DEFAULT_BRANCH`, `refs/heads/release/*`). Preserved as written."
      },
      "match_kind": {
        "type": "string",
        "enum": [
          "declared",
          "resolved"
        ],
        "description": "`declared` \u2014 the ruleset applies to the repository and its pattern is recorded verbatim. `resolved` \u2014 the pattern was matched against a ref actually observed on the grid. A view that shows what protects `main` today wants resolved; an audit of intent wants declared."
      }
    }
  }
}
